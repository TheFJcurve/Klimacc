# Given a particular button and page, this will give instruction on what to do once the button is pressed


def mainPageButton(window):
    window.destroy()
    import userWrapper


def userWrapperSignIn(window):
    window.destroy()
    import login


def userWrapperSignUp(window):
    window.destroy()
    import signUp
